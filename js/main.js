$(document).ready(function () {
    $(".same-slider").slick({
        // dots: false,
        // infinite: true,

    });
    $(".same-slide").slick({
        // dots: false,
        // infinite: true,

    });

})
